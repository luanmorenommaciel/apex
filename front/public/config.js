// Development placeholder. The production image OVERWRITES this file at
// container start from its own environment; leaving it empty here makes the dev
// server and a plain `npm run build` fall through to import.meta.env.
// See src/data/runtimeConfig.ts for the resolution order.
window.__APEX_CONFIG__ = {};
