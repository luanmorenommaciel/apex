---
id: T-20260820-l6-docs
title: "Document cross-run memory across the lane docs"
status: done
format_version: 3
profile: standard
effort: M
budget_iterations: 15
agent: any
parent: (none)
depends_on: [T-20260820-l6-tool-surface, T-20260820-noise-floor-honesty]
touches_paths: [serve/README.md, serve/VALIDATION.md, docs/lanes/SERVE-LEGS.md]
creates_paths: []
source_note: "docs/lanes/SERVE-LEGS.md"
created: "2026-08-20T00:00:00Z"
tags: []
owner: (none)
priority: P2
severity: feature
due_date: (none)
precondition: (none)
blocked_reason: (none)
security_class: (none)
source_action_item: (none)
tracker_ref: (none)
execution_backend: any
signed_off: true
signed_off_by: sidymar.prexede@gmail.com
signed_off_at: "2026-08-20T00:00:00Z"
accepted: true
accepted_by: sidymar.prexede@gmail.com
accepted_at: "2026-08-20T00:00:00Z"
---

# Document cross-run memory across the lane docs

> **Why:** SERVE-LEGS still describes L6 as building an embedding backend, which would duplicate a lane that already exists with 47 tests and a normalised embedding table.

## Goal

Record that L6 is an MCP surface over the memory lane, not a reimplementation.

## Context

Rewrite the L6 section so the next reader does not rebuild what memory already does.

## Behavior

- **B-1** — GIVEN serve/README.md WHEN read THEN recall_similar_runs is in the tool table and the floor rule is stated
- **B-2** — GIVEN serve/VALIDATION.md WHEN read THEN it records a test count copied from an actual run
- **B-3** — GIVEN docs/lanes/SERVE-LEGS.md WHEN read THEN L6 is described as exposing the memory lane, naming apex.plan_memory and apex.run_outcomes

## Success Criteria

```bash
# eval_1: the lane docs describe L6 as a surface over memory
eval_1() {
  ( grep -q 'plan_memory' docs/lanes/SERVE-LEGS.md && grep -q 'recall_similar_runs' serve/README.md )
}

# eval_2: the recorded test count equals what the suite reports
eval_2() {
  ( cd serve && n=$(uv run --extra dev pytest 2>&1 | grep -oE '[0-9]+ passed' | grep -oE '[0-9]+') && grep -q "$n passed" VALIDATION.md )
}

```

## Validation Card

```yaml
success_criteria:
  - id: eval_1
    description: "the lane docs describe L6 as a surface over memory"
    runnable: bash
    check_type: deterministic
    verifies: [B-1, B-3]
    terminal: true
    expected_duration_sec: 10
  - id: eval_2
    description: "the recorded test count equals what the suite reports"
    runnable: bash
    check_type: deterministic
    verifies: [B-2]
    terminal: true
    expected_duration_sec: 60
retry_policy:
  max_iterations: 15
  circuit_breaker_no_progress: 3
  on_terminal_failure: park_with_context
agent_contract:
  version: 2
  read: [intent, behavior, contract, guardrails]
  produce: [code, tests]
  required_tools: [git, bash]
  timeout_minutes: 30
  sandbox_type: host
  output_artifacts: []
  mcp_dependencies: []
  emit: [pass, fail, retry_with_reason, parked_with_context]
  backend_metadata: {}
```

## Exit Check

```bash
eval_1 && eval_2
```

## Rollback Plan

Revert only the declared write surface and park the task with context.

## Observability Hooks

(none — no runtime observability required)

## Anti-Patterns

- Writing a test count from memory.
- Leaving the L6 section describing work the memory lane already did.

## Do-Not-Touch

- `serve/src/apex_mcp`
- `serve/tests`
- `docs/lanes/SERVE.md`

## Open Questions

(none — this task is fully specified)
